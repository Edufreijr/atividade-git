name: conversa

on:
  push:
    branches:
      - main

jobs:
  escreve-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Nosso primeiro WOrkflow
        run: echo "Nosso Primeiro workflow"